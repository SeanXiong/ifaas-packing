# IFAAS 项目打包工具产品说明书

> 文档版本：V1.0
> 对应代码分支：`1.0`
> 编制日期：2026-08-11

## 1. 产品概述

IFAAS 项目打包工具是一套面向项目交付、实施和研发人员的轻量级 Web 打包工作台。用户可在浏览器中完成项目、版本和服务组件选择，调整组件分支及端口映射，并创建安装包或升级包。

工具自身不执行源码编译或制品构建，而是通过本地 Python 服务将浏览器请求代理至 IFAAS 后端，由后端执行项目数据查询、组件维护、打包和 Seafile 上传等业务操作。

### 1.1 产品目标

- 将项目、版本、组件和打包参数集中到统一操作界面。
- 降低手工拼装打包 API 参数的出错概率。
- 支持安装包与升级包、在线包与离线包的统一制作。
- 提供打包记录、下载地址、文件校验值和云盘上传进度查询。
- 以 Python 标准库和原生 Web 技术运行，减少部署依赖。

### 1.2 适用角色

| 角色 | 典型用途 |
| --- | --- |
| 项目交付人员 | 按客户或现场版本选择组件并制作交付包 |
| 实施运维人员 | 获取制品、核对 MD5、上传 Seafile、调整部署端口 |
| 研发人员 | 切换组件分支或标签后发起验证打包 |
| 系统管理员 | 配置后端地址、监听端口和部署代理 |

## 2. 产品范围

### 2.1 已支持能力

- 账号密码登录、Token 会话保持和主动退出。
- 按账号保存登录资料，并通过 URL 参数预填指定账号。
- 收藏项目、全量项目搜索和分页浏览。
- 项目、版本、业务组件三栏联动。
- 版本搜索、组件搜索、当前筛选结果全选。
- 组件分支或标签切换。
- 组件端口映射查看与修改。
- 业务组件删除。
- 安装包、升级包制作。
- 在线包、离线包制作。
- `x86_64`、`aarch64` CPU 架构选择。
- 命名空间设置。
- 打包参数和组件分支二次确认。
- 按包类型、网络类型查看打包记录。
- 查看打包状态、模块、基础信息、下载地址和 MD5。
- 删除打包记录。
- 下载内网制品、复制内网或云盘地址。
- 离线制品上传 Seafile，并显示进度和速度。

### 2.2 不在本工具范围内

- 用户、角色和权限管理。
- 项目、版本或组件的新建。
- 源码编辑、代码评审和流水线配置。
- 后端打包引擎、制品仓库或 Seafile 的运维。
- 对后端返回的业务数据进行长期归档。

## 3. 系统组成

```text
浏览器
  │
  ├─ 静态页面、交互和会话 Token
  │
  ▼
Python 本地服务（server.py）
  ├─ 提供 HTML、CSS、JavaScript 静态资源
  ├─ 读写 config/*.json 本地配置
  └─ 代理 /api/proxy/* 请求
  │
  ▼
IFAAS 后端 API
  ├─ 登录认证
  ├─ 项目、版本和组件数据
  ├─ 安装包、升级包任务
  └─ 制品与 Seafile 上传任务
```

Python 服务采用多线程请求处理方式。普通代理请求默认超时为 30 秒；安装包和升级包提交请求不设置客户端代理超时，以适配长时间打包调用。

## 4. 运行环境

### 4.1 客户端

- 支持原生 JavaScript、`sessionStorage` 和 Clipboard API 的现代浏览器。
- 浏览器能够访问本工具的 Web 地址。

### 4.2 服务端

- Python 3。
- 无需安装第三方 Python 包，当前运行逻辑仅使用标准库。
- 服务端能够访问 `config/server.json` 指定的 IFAAS 后端地址。
- 如需云盘上传，IFAAS 后端必须已正确配置 Seafile。

### 4.3 启动方式

在项目根目录执行：

```bash
python server.py
```

浏览器访问服务启动日志中显示的地址。默认配置缺失或无效时，服务回退监听 `127.0.0.1:8080`。

## 5. 登录与会话

### 5.1 登录

1. 打开项目打包客户端。
2. 输入 IFAAS 账号和密码。
3. 单击“登录”，或在密码输入框中按 Enter。
4. 登录成功后进入“项目打包工作台”，系统自动加载当前账号的收藏项目。

登录成功后，Token 保存在当前浏览器标签页的 `sessionStorage` 中。刷新页面时，工具会恢复当前会话；关闭标签页后不会跨会话长期保存 Token。

当后端返回 HTTP 401、HTTP 403 或可识别的 Token 无效信息时，工具会清除会话并返回登录页。

### 5.2 登录资料预填

登录成功后，工具会按账号将用户名和密码写入 `config/login-profiles.json`。访问以下地址时，可自动预填对应账号：

```text
http://服务地址/?profile=账号
```

账号仅允许字母、数字、下划线和连字符。若资料不存在、格式无效或读取失败，登录表单保持为空，不影响手工登录。

### 5.3 个人信息与退出

- 单击右上角账号区域，可查看当前登录账号。
- 单击“退出登录”后，工具请求后端退出接口；请求成功后清除本地会话并返回登录页。
- 若后端退出请求失败，页面会提示错误并保留当前本地会话，用户可关闭当前浏览器标签页结束标签页会话。

## 6. 工作台说明

工作台采用项目、版本、组件三栏联动布局，各栏宽度可拖动调整。

| 区域 | 主要功能 |
| --- | --- |
| 项目管理 | 切换收藏/全量项目、搜索、分页、收藏或取消收藏 |
| 版本选择 | 显示项目版本、搜索版本、选择版本、查看打包记录 |
| 组件配置与打包 | 筛选和勾选组件、切换分支、修改端口、删除组件、开始打包 |

### 6.1 项目管理

“我的收藏”按当前登录账号从本地配置读取；“全量项目”从 IFAAS 后端分页查询。

- 在搜索框输入项目名称，系统进行延迟搜索，也可按 Enter 立即查询。
- 单击项目可加载该项目的版本列表。
- 使用项目旁的收藏按钮可收藏或取消收藏。
- 收藏内容按账号隔离，保存在 `config/favorites.json`。
- 收藏仅缓存项目基本信息；单击收藏项目时仍实时查询版本。

### 6.2 版本选择

- 选择项目后，中栏显示其版本列表。
- 在“搜索版本”中输入关键字，可过滤当前已加载版本。
- 单击版本可加载业务组件。
- 通过版本对应的记录入口可查看打包记录；首次进入默认查询“离线升级包”。

### 6.3 组件选择

- 列表展示服务名、服务版本和可用操作。
- 输入服务名或分支关键字可过滤组件。
- “全选”仅作用于当前过滤后可见的组件。
- 至少勾选一个组件后才能提交打包。
- 单击“取消”会清空当前组件区域和勾选状态。

## 7. 组件管理

组件管理操作会直接调用 IFAAS 后端并修改当前版本数据，操作前应确认目标项目和版本。

### 7.1 切换分支

1. 在组件行单击“切换分支”。
2. 工具根据组件的 Git 地址查询可用分支和标签。
3. 选择目标分支或标签。
4. 工具查询该分支的 Git 配置。
5. 确认保存后，更新组件并刷新当前行显示的分支。

若组件缺少 `git_url`、后端未返回有效 `git_id`，或 Git 配置查询失败，则不能保存分支。

### 7.2 修改端口映射

1. 在组件行单击“端口列表”。
2. 查看原端口，并修改对应的映射端口。
3. 单击“保存”，或在输入框中按 Enter。

映射端口必须为 1 至 65535 之间的整数。组件未返回端口记录时，仅显示“暂无端口配置”，保存按钮不可用。

### 7.3 删除组件

1. 在组件行单击“删除”。
2. 核对二次确认提示。
3. 单击“确认删除”。

删除成功后，组件会从当前版本和页面列表移除。该操作不可恢复。

## 8. 创建打包任务

### 8.1 操作流程

1. 依次选择项目和版本。
2. 根据需要切换组件分支或修改端口。
3. 勾选一个或多个业务组件。
4. 单击“开始打包”。
5. 设置打包参数并单击“下一步”。
6. 核对包类型、网络类型、CPU 架构、命名空间、云盘选项，以及组件和分支。
7. 单击“确认打包”。
8. 提交成功后，工具自动打开本次包类型对应的打包记录。

### 8.2 打包参数

| 参数 | 默认值 | 可选值或规则 |
| --- | --- | --- |
| 包类型 | 升级包 | 升级包、安装包，单选 |
| 网络类型 | 离线 | 离线、在线，单选 |
| CPU 架构 | `x86_64` | `x86_64`、`aarch64`，单选 |
| 命名空间 | `basic-app` | 可输入；空值提交时回退默认值 |
| 上传云盘 | 否 | 仅离线包可选；在线包自动关闭并禁用 |

提交时，所有已选业务组件固定携带 `need_apollo: true`。当前界面不提供中间件、资源、操作系统或其他高级打包对象的手工配置入口。

### 8.3 包类型说明

| 组合 | 后端业务 | 云盘上传 |
| --- | --- | --- |
| 离线升级包 | 调用升级包接口，`offline=1` | 支持 |
| 在线升级包 | 调用升级包接口，`offline=0` | 不支持 |
| 离线安装包 | 调用安装包接口，`offline=1` | 支持 |
| 在线安装包 | 调用安装包接口，`offline=0` | 不支持 |

## 9. 打包记录与制品

### 9.1 记录筛选

打包记录窗口支持按以下条件重新查询：

- 包类型：升级包、安装包。
- 网络类型：离线、在线。

### 9.2 状态说明

| 后端状态值 | 页面状态 | 说明 |
| --- | --- | --- |
| `0` | 打包中 | 任务正在处理 |
| `1` | 成功 | 制品已生成 |
| `2` | 失败 | 打包未成功 |
| `3` | 打包中 | 任务仍在处理 |
| 其他 | 原始状态值 | 后端返回了未知状态 |

### 9.3 记录内容

每条记录可展示：

- 包名称、创建时间、打包人、CPU 架构和包含模块数。
- 内网下载地址、云盘地址。
- 命名空间、操作系统、平台、离线类型、网盘状态和文件 MD5。
- 模块名称、版本分支、配置中心状态及模块详细信息。

单击记录摘要可展开模块和基础信息；单击模块卡片可打开模块详情。

### 9.4 下载与复制

- 内网地址存在时，可直接下载制品或复制地址。
- 云盘地址存在时，可复制云盘地址。
- 下载能力取决于浏览器能否访问后端返回的制品地址。

### 9.5 上传 Seafile

对于尚无云盘地址的离线包，可单击“上传云盘”：

1. 工具使用记录中的 `storage_path` 创建上传任务。
2. 每 2 秒查询一次任务进度。
3. 页面显示百分比、描述和后端提供的上传速度。
4. 上传完成后自动刷新打包记录并显示云盘地址。

单次进度查询失败不会立即终止任务轮询。在线包不显示“上传云盘”按钮。

### 9.6 删除记录

单击记录中的“删除”并二次确认后，工具会删除对应安装包或升级包记录。该操作不可恢复；是否同时清理后端制品文件取决于 IFAAS 后端实现。

## 10. 配置说明

所有本地运行配置均位于 `config/` 目录。

| 文件 | 用途 | 维护方式 |
| --- | --- | --- |
| `server.json` | 后端地址、监听地址和端口 | 管理员手工维护 |
| `favorites.json` | 各账号收藏的项目对象 | 页面自动读写 |
| `login-profiles.json` | 各账号的登录名和密码 | 登录成功后自动写入 |

`server.json` 字段如下：

```json
{
  "backend_url": "http://后端地址:端口",
  "listen_host": "127.0.0.1",
  "listen_port": 8080
}
```

| 字段 | 说明 |
| --- | --- |
| `backend_url` | IFAAS 后端根地址，末尾不要求 `/` |
| `listen_host` | Python 服务监听地址；本机使用可设为 `127.0.0.1` |
| `listen_port` | Python 服务监听端口 |

修改 `server.json` 后需重启 Python 服务。

## 11. 部署建议

### 11.1 个人本机运行

- 将 `listen_host` 设置为 `127.0.0.1`。
- 直接运行 `python server.py`。
- 仅由本机浏览器访问。

### 11.2 服务器部署

推荐使用 Systemd 管理 Python 服务，并使用 Nginx 提供统一访问入口：

```text
外部浏览器 → Nginx → Python 服务 → IFAAS 后端
```

如果 Nginx 位于 Docker 容器中，Python 服务需监听容器能够访问的宿主机地址，例如 `0.0.0.0`，并通过防火墙限制可访问来源。具体命令和 Nginx 示例见项目根目录的《部署文档》。

## 12. 安全说明

当前版本定位为受控网络内的轻量工具，部署前必须了解以下边界：

- `login-profiles.json` 以明文保存登录密码，应严格限制文件权限和备份范围。
- `/api/config/*` 配置读写接口自身没有独立认证保护。
- Python 服务为响应添加 `Access-Control-Allow-Origin: *`。
- 本工具没有自身的角色权限模型，业务权限依赖 IFAAS 后端 Token。
- 不应将 Python 服务端口直接暴露到互联网或不可信网络。
- 推荐仅绑定本机，或置于带访问控制、TLS 和可信来源限制的反向代理之后。
- 日志、截图和问题反馈中不得包含密码、Token、内部下载地址或其他敏感数据。

## 13. 常见问题

### 13.1 登录后立即返回登录页

可能原因：账号无效、Token 已失效、后端返回 HTTP 401/403，或后端认证响应中缺少 `key`。请先验证账号，再检查后端登录接口。

### 13.2 项目、版本或组件加载失败

检查：

- `backend_url` 是否正确。
- Python 服务到 IFAAS 后端的网络是否连通。
- 当前账号是否有目标项目权限。
- 浏览器或 Python 服务日志中的 HTTP 状态和错误信息。

### 13.3 “开始打包”不可用

需先选择版本并成功加载至少一个组件。提交时还必须勾选至少一个业务组件。

### 13.4 无法切换分支

确认组件含有效 Git 地址，Git 服务可访问，后端分支查询接口能返回分支或标签，且目标分支存在有效 Git 配置。

### 13.5 端口无法保存

确认映射端口为 1 至 65535 之间的整数，并且端口记录含 `id`、`version`、`module` 和 `default_port` 等必要字段。

### 13.6 打包提交长时间无响应

安装包和升级包接口允许长时间运行。请避免重复提交，并检查 Python 服务日志及 IFAAS 后端任务状态。

### 13.7 云盘上传按钮未显示

仅离线包允许上传 Seafile；已有云盘地址时显示复制按钮；记录缺少 `storage_path` 时无法触发上传。

### 13.8 Nginx 返回 502

确认 Python 服务正在运行、监听地址允许 Nginx 容器访问，并检查 Nginx `proxy_pass` 指向的宿主机地址和端口。

## 14. 操作注意事项

- 提交前务必核对项目、版本、包类型、CPU 架构和组件分支。
- 组件删除和打包记录删除均不可恢复。
- 修改分支或端口属于当前版本数据变更，不只是本地临时选择。
- 过滤后执行“全选”只会选择当前可见组件。
- 在线包不能上传 Seafile。
- 打包成功提示表示后端已接受任务，不等同于制品已经生成；最终结果以打包记录状态为准。
- 下载或交付前建议核对文件 MD5、CPU 架构和模块清单。

## 15. 验收检查表

- [ ] 能使用有效 IFAAS 账号登录并主动退出。
- [ ] 刷新页面后能恢复当前标签页会话。
- [ ] 能搜索、分页和收藏项目。
- [ ] 能选择项目、版本并加载组件。
- [ ] 能过滤、勾选和全选可见组件。
- [ ] 能查询分支或标签并保存组件分支。
- [ ] 能查看和修改合法端口映射。
- [ ] 能分别提交四种包类型组合。
- [ ] 在线包不会启用云盘上传。
- [ ] 能按包类型和网络类型查询打包记录。
- [ ] 能查看状态、模块、MD5 和下载地址。
- [ ] 能下载或复制内网制品地址。
- [ ] 能上传离线包至 Seafile 并看到进度。
- [ ] 删除组件和记录前均有二次确认。
- [ ] 配置文件权限和网络访问范围符合安全要求。

## 16. 相关文档

- `README.md`：项目简介和本地启动说明。
- `部署文档.md`：Systemd、Docker Nginx 部署及问题排查。
- `apiInfo/`：后端接口调用样例，仅供开发和联调参考。
