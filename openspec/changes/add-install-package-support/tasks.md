## 1. 包类型模型与接口路由

- [x] 1.1 在前端定义统一的包类型映射，覆盖升级/安装与离线/在线的显示名称、提交、记录和删除资源。
- [x] 1.2 扩展 `ApiClient`，支持安装包提交、安装包记录查询和安装包记录删除，并让现有升级包调用通过统一包类型映射执行。
- [x] 1.3 将 `/api/v1/packplus/install/` 加入本地代理长请求路径。
- [x] 1.4 为四类包的接口映射和安装包代理长请求策略补充自动化测试。

## 2. 打包交互与云盘限制

- [x] 2.1 在开始打包后实现升级包/安装包选择弹窗，并将选择结果与当前离线/在线状态固化为最终包类型。
- [x] 2.2 更新二次确认弹窗，展示完整包类型、所选组件和组件分支，并按确认时快照提交请求。
- [x] 2.3 在选择在线方式时取消并禁用云盘上传开关，且在请求构建阶段强制在线包传递 `seafile: false`。
- [x] 2.4 调整相应样式与无障碍状态，清晰表达包族选择、禁用云盘开关和加载状态。
- [x] 2.5 为包族选择、确认内容和在线包云盘限制补充前端逻辑测试。

## 3. 打包记录与离线云盘上传

- [x] 3.1 将打包记录弹窗改为默认离线升级包的类型下拉查询，并移除搜索、状态、CPU 架构和优先级排序控件及其本地过滤逻辑。
- [x] 3.2 让记录类型切换、删除完成和上传完成均重新加载当前选中的包类型记录，且按后端返回顺序展示。
- [x] 3.3 按记录的包类型调用升级包或安装包删除接口。
- [x] 3.4 仅对离线且无云盘地址的记录显示上传云盘操作；在线包隐藏上传入口。
- [x] 3.5 在上传进度区域展示 `progress.speed`，字段缺失时保持现有进度显示，完成或失败后清除速度文本。
- [x] 3.6 为记录类型查询、安装包删除、在线包上传隐藏和上传速度展示补充自动化测试。

## 4. 验证与交付

- [x] 4.1 运行现有及新增的 JavaScript 测试、语法检查和差异检查。
- [ ] 4.2 手动验证四类包的提交请求、默认记录查询、安装包删除、离线安装包上传速度和在线包云盘限制。
- [x] 4.3 更新静态资源缓存版本，并在交付说明中记录部署时需要重启本地代理服务。
