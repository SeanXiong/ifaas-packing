## 1. 跨系统契约与旧方案收敛

- [x] 1.1 以当前部署页面的现有逻辑确认项目、版本、服务、Git refs、Git 配置、模块更新、组合打包上传、独立 Seafile 上传和进度查询入口。
- [x] 1.2 记录非生产环境真实请求与响应样例，确定项目、版本、服务、打包记录、云盘地址和上传进度的字段白名单。
- [x] 1.3 在缺少后端源码时采用保守策略，将服务锁持有至打包请求返回并查询到新增制品记录。
- [x] 1.4 定义构建平台发布任务、打包平台设置、自动任务创建/查询及稳定错误结构的 JSON Schema。
- [x] 1.5 移除 `add-codex-automated-package-release` 旧规划文件，并停用与本 change 冲突的项目/版本逐次选择、构建平台切分支、Commit 对账和 MCP 必选任务，禁止两套入口同时启用。
- [x] 1.6 在打包平台后端和 `GitLabBuildWatch` 仓库分别创建或更新引用本 change 的 OpenSpec 变更。

## 2. 打包平台自动目标设置服务端

- [x] 2.1 定义全局自动目标配置模型，包含 `projectId`、`projectName`、`versionId`、`versionName`、创建和最后修改字段。
- [x] 2.2 实现配置读取接口，返回未配置、有效和失效状态且不泄露账号凭据。
- [x] 2.3 实现项目分页搜索候选接口，归一化返回项目名称和 `projectId`。
- [x] 2.4 实现按 `projectId` 查询和搜索版本候选接口，界面使用产品文案但接口返回 `versionId`。
- [x] 2.5 实现配置保存接口，在服务端重新校验项目存在、版本存在且版本属于项目。
- [x] 2.6 实现首次创建和后续修改审计规则，保留创建字段并只刷新最后修改字段。
- [x] 2.7 实现配置失效检测及自动任务创建时的 `AUTOMATION_TARGET_NOT_CONFIGURED`、项目不存在和版本不存在错误。
- [x] 2.8 增加设置服务端测试，覆盖首次保存、修改、非法 ID、跨项目版本、候选搜索和配置失效。

## 3. 打包平台右上角设置 UI

- [x] 3.1 调整打包工作台 Header，在用户菜单前增加自动打包配置状态和带图标的“设置”按钮。
- [x] 3.2 参考构建平台实现桌面约 460px、移动端自适应宽度的右侧设置抽屉。
- [x] 3.3 实现项目 Combobox，支持输入防抖、分页远程匹配、加载、空结果、错误重试和名称加 ID 展示。
- [x] 3.4 实现产品 Combobox，在项目未选择时禁用，并按所选项目搜索现有版本候选。
- [x] 3.5 实现项目切换时清空产品，禁止把未选择候选的自由文本保存为 ID。
- [x] 3.6 在抽屉中只读展示创建人、创建时间、最后修改人和最后修改时间。
- [x] 3.7 实现保存、取消、服务端校验错误和成功后 Header 配置状态刷新。
- [x] 3.8 实现未配置和配置失效状态点击后直接打开设置抽屉，并在自动任务不可用时展示明确提示。
- [x] 3.9 增加 UI 自动化测试，覆盖级联选择、输入匹配、切换项目清空产品、非法自由文本、保存和失效状态。

## 4. 打包平台异步任务与持久化

- [x] 4.1 定义自动任务持久化 Schema、`schemaVersion`、唯一 `packageTaskId`、`clientRequestId` 索引、请求和目标快照。
- [x] 4.2 定义并实现 `ACCEPTED`、目标发现、排队、分支校准、组合打包上传、云盘验证、补偿和终态迁移。
- [x] 4.3 实现原子任务写入与损坏记录处理，只有完整落盘后才向后台执行器提交任务。
- [x] 4.4 实现自动任务创建接口，以 `clientRequestId` 幂等返回 `202 Accepted` 和唯一 `packageTaskId`。
- [x] 4.5 实现按 `packageTaskId` 查询接口，返回真实阶段、进度、目标、队列、分支、制品、云盘和结构化错误。
- [x] 4.6 实现任务创建时复制全局项目与版本配置快照，禁止运行中重新读取新配置改变目标。
- [x] 4.7 实现进程启动时非终态任务扫描和幂等恢复，避免重复创建包或补偿上传。
- [x] 4.8 增加任务创建、响应丢失重试、精确查询、原子写入失败、损坏记录和重启恢复测试。

## 5. 服务发现、FIFO 队列和服务锁

- [x] 5.1 复用并完善 Git URL 规范化，以配置 `versionId` 下的服务 `git_url` 唯一匹配请求仓库。
- [x] 5.2 实现零匹配 `SERVICE_NOT_FOUND` 和多匹配 `SERVICE_TARGET_AMBIGUOUS`，并固化唯一服务快照。
- [x] 5.3 实现以 `versionId:serviceId` 为键的持久化 FIFO 队列、锁所有者和队列位置查询。
- [x] 5.4 实现前序任务释放锁后只唤醒最早同键任务，不同锁键任务允许并行。
- [x] 5.5 按当前页面打包调用链将锁持有至打包请求返回并查询到新增制品记录，随后在云盘验证前释放。
- [x] 5.6 实现异常、超时和进程重启时的锁恢复与孤儿锁处理，禁止同键任务并行恢复。
- [x] 5.7 增加唯一匹配、无匹配、多匹配、FIFO 顺序、不同服务并行、锁释放和重启恢复测试。

## 6. 分支校准

- [x] 6.1 实现目标 branch 存在性及 Git 配置校验，缺少 branch 或 `git_id` 时返回稳定错误。
- [x] 6.2 实现服务当前分支一致时跳过更新并记录 `changed=false`、`verified=true`。
- [x] 6.3 实现读取完整服务、提交目标 Git 配置和重新查询验证的分支切换流程。
- [x] 6.4 保存 `previousBranch`、`targetBranch`、`changed`、`verified`，并确保成功或失败后均不自动恢复原分支。
- [x] 6.5 增加分支已一致、切换成功、目标不存在、更新失败和验证失败测试。

## 7. 组合打包上传与云盘补偿

- [x] 7.1 将自动任务接入现有安装包/升级包 Service，并在原请求中直接传递云盘上传选项。
- [x] 7.2 在任务创建阶段校验包类型、网络类型、CPU 架构、namespace 和云盘组合，禁止在线包请求云盘上传。
- [x] 7.3 打包完成后重新查询记录，归一化包名称、下载地址、MD5、`cloudPath` 和 `cloudUrl`。
- [x] 7.4 云盘地址暂缺时先查询组合上传是否仍在进行，并进行有界等待而不重复上传。
- [x] 7.5 确认没有进行中上传且地址缺失时，以 `packageTaskId:cloud-recovery` 幂等触发一次现有独立上传能力。
- [x] 7.6 补偿完成后重新查询结果，仍无地址时以 `CLOUD_ADDRESS_MISSING` 失败且不得无限重试。
- [x] 7.7 实现 `uploadCloud=true` 必须存在可用云盘地址、`uploadCloud=false` 只需包成功的终态校验。
- [x] 7.8 增加组合成功、结果延迟、上传进行中、补偿成功、补偿重试幂等、补偿失败和在线包拒绝测试。

## 8. 构建平台发布任务与打包衔接

- [x] 8.1 在 `GitLabBuildWatch` SQLite 中新增发布任务、请求快照、Pipeline、`packageTaskId`、结果、通知和错误字段及迁移。
- [x] 8.2 实现发布任务登记接口，按 `clientRequestId` 幂等保存完整 Commit SHA 和打包参数并返回 `READY_TO_PUSH`。
- [x] 8.3 实现 push 失败报告和 `PUSH_FAILED` 终态，禁止该任务继续创建打包任务。
- [x] 8.4 复用 GitLab 客户端按 Project、branch 和完整 Commit SHA 查找 Pipeline，禁止其他成功 Commit 误触发。
- [x] 8.5 目标 Pipeline 成功后以 `releaseTaskId` 作为打包平台 `clientRequestId` 创建任务并保存 `packageTaskId`。
- [x] 8.6 实现按 `packageTaskId` 有界轮询、队列与真实阶段展示、网络重试和进程重启恢复。
- [x] 8.7 终态时保存目标、分支、包、云盘、MD5 和错误快照并停止轮询。
- [x] 8.8 增加登记幂等、目标 SHA 成功/失败、其他 Commit 不误触发、创建响应丢失、轮询恢复和结果快照测试。

## 9. 构建平台工作流与 Windows 通知

- [x] 9.1 在构建平台发布任务列表和详情展示登记、推送、构建、排队、分支校准、打包上传、补偿和完成阶段。
- [x] 9.2 展示队列位置、项目、产品、服务、分支变化、目标 Pipeline 和结构化失败原因。
- [x] 9.3 成功时发送包含服务、产品、云盘路径和可点击 `cloudUrl` 的 Windows Toast。
- [x] 9.4 失败时在通知中展示准确阶段和简明原因。
- [x] 9.5 通知失败时单独记录 `notificationStatus`，不得修改已经完成的发布主状态。
- [x] 9.6 增加页面关闭后台通知、通知去重、链接打开和历史快照展示测试。

## 10. 显式发布 Skill 与 CLI

- [x] 10.1 简化 `ifaas-release` Skill 描述和触发规则，使其只在用户显式调用 `$ifaas-release` 时执行完整流程。
- [x] 10.2 对未显式调用 Skill 的普通修改、提交、推送和自然语言打包请求增加不触发验收场景。
- [x] 10.3 实现本地 CLI/脚本读取规范化 remote、当前 branch、完整 Commit SHA 和明确打包参数。
- [x] 10.4 实现 CLI/脚本从模型上下文之外读取构建平台地址和凭据，并以结构化 JSON 调用发布登记接口。
- [x] 10.5 实现本地检查与 commit 后先登记、收到 `READY_TO_PUSH` 后再 push 的门禁流程。
- [x] 10.6 实现登记失败禁止 push、push 失败回报构建平台、成功后输出 `releaseTaskId` 和等待状态。
- [ ] 10.7 在未安装或未注册 MCP 的环境完成 Skill 端到端验收，确认 MCP 不是运行依赖。

## 11. 跨系统验收、部署和文档

- [ ] 11.1 在非生产项目配置真实项目与产品，验证可搜索级联选择、服务唯一匹配和配置快照隔离。
- [ ] 11.2 验证只有显式 `$ifaas-release` 触发，任务先登记再 push，构建平台只接受目标 Commit Pipeline。
- [ ] 11.3 验证同一服务多个任务按 FIFO 排队、不同服务并行、分支自动校准且任务结束不恢复。
- [ ] 11.4 验证组合打包上传正常路径不二次上传，云盘地址缺失时只补偿一次并返回最终地址。
- [ ] 11.5 分别重启构建平台和打包平台，验证不丢任务、不重复打包、不重复补偿上传且锁顺序不变。
- [x] 11.6 更新打包平台、构建平台和 Skill 使用文档，记录配置、状态、错误、凭据、部署、回滚和人工恢复方法。
- [ ] 11.7 禁用旧方案冲突入口后完成灰度验收，保留 Web 手工打包作为回退路径。
