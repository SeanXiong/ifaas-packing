# IFAAS Packing 工具部署文档

> 当前部署不使用 Nginx：Web 服务直接暴露 `36001`，MCP 服务直接暴露 `36003`。
> 下文 Docker Nginx 内容仅作为未来需要统一入口或 HTTPS 时的可选方案。

## 当前直接暴露端口方案

```text
浏览器/Codex
    ├── http://服务器IP:36001       IFAAS Packing Web
    └── http://服务器IP:36003/mcp   IFAAS MCP
```

Web 服务配置：

```json
{
  "backend_url": "http://192.168.12.35:3000",
  "listen_host": "0.0.0.0",
  "listen_port": 36001
}
```

MCP 服务环境变量：

```ini
IFAAS_MCP_ACCESS_TOKEN=<高强度随机令牌>
IFAAS_PACKING_URL=http://127.0.0.1:36001
IFAAS_MCP_HOST=0.0.0.0
IFAAS_MCP_PORT=36003
```

验证：

```bash
curl http://127.0.0.1:36001
curl http://127.0.0.1:36003/health
ss -lntp | grep -E '36001|36003'
```

## 一、部署架构

系统采用如下部署架构：

```text
                外部浏览器
                     │
                     │ HTTP:36002
                     ▼
           Docker Nginx（反向代理）
                     │
                     │ proxy_pass
                     ▼
        宿主机 Python 服务（36001）
                     │
               IFAAS Packing
```

其中：

* **Python 服务**：运行于宿主机，由 Systemd 管理。
* **Nginx**：运行于 Docker 容器，对外提供统一访问入口。
* **浏览器**：仅访问 Nginx 暴露的 36002 端口。

---

# 二、Python 服务部署

主 Web 与自动打包服务支持 Python 3.6.9 及以上版本且不需要安装第三方依赖。部署前先确认：

```bash
/usr/bin/python3 --version
```

可选 MCP 服务不属于自动发布主链路，其第三方依赖和 Python 版本要求应独立管理。

## 1、Systemd 配置

创建服务：

```bash
sudo vim /etc/systemd/system/ifaas-packing.service
```

配置内容：

```ini
[Unit]
Description=IFAAS Packing Web Service
After=network.target

[Service]
Type=simple
User=intellif
WorkingDirectory=/home/intellif/xf/ifaas-packing
ExecStart=/usr/bin/python3 /home/intellif/xf/ifaas-packing/server.py
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
```

---

## 2、加载服务

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now ifaas-packing
```

---

## 3、常用命令

启动：

```bash
sudo systemctl start ifaas-packing
```

停止：

```bash
sudo systemctl stop ifaas-packing
```

重启：

```bash
sudo systemctl restart ifaas-packing
```

查看状态：

```bash
sudo systemctl status ifaas-packing
```

实时日志：

```bash
sudo journalctl -u ifaas-packing -f
```

---

# 三、Python 服务监听配置

`server.py` 中读取：

```python
host = config["listen_host"]
port = config["listen_port"]
```

因此监听地址由配置文件决定。

推荐：

```json
{
    "listen_host": "0.0.0.0",
    "listen_port": 36001
}
```

**不要配置为：**

```json
"listen_host": "127.0.0.1"
```

否则 Docker 容器无法访问宿主机服务。

修改后验证：

```bash
ss -lntp | grep 36001
```

正确结果：

```text
LISTEN 0 5 0.0.0.0:36001
```

---

# 四、Docker Nginx 部署

建议端口映射：

```text
宿主机 36002
        │
        ▼
容器 36002
```

例如：

```yaml
ports:
  - "36002:36002"
```

---

# 五、Nginx 配置

推荐配置：

```nginx
worker_processes 1;

events {
    worker_connections 1024;
}

http {

    sendfile on;

    include /etc/nginx/mime.types;

    charset utf-8;

    gzip off;

    server {

        listen 36002;

        server_name _;

        client_max_body_size 500m;

        location / {

            proxy_pass http://172.17.0.1:36001;

            proxy_http_version 1.1;

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            proxy_connect_timeout 30s;
            proxy_send_timeout 3600s;
            proxy_read_timeout 3600s;

            proxy_buffering off;
        }
    }
}
```

说明：

* `172.17.0.1` 为 Docker 默认桥接网络访问宿主机地址。
* 如果 Docker 网络发生变化，可通过：

```bash
ip addr show docker0
```

查看宿主机桥接地址。

---

# 六、Nginx 验证

检查配置：

```bash
docker exec nginx-swagger nginx -t
```

重新加载：

```bash
docker exec nginx-swagger nginx -s reload
```

查看完整配置：

```bash
docker exec nginx-swagger nginx -T
```

查看日志：

```bash
docker logs nginx-swagger
```

---

# 七、验证步骤

## 1、验证 Python 服务

宿主机：

```bash
curl http://127.0.0.1:36001
```

应正常返回。

---

## 2、验证 Docker 是否能够访问宿主机

进入容器：

```bash
docker exec -it nginx-swagger bash
```

执行：

```bash
curl http://172.17.0.1:36001
```

应正常返回。

---

## 3、验证 Nginx

宿主机：

```bash
curl http://127.0.0.1:36002
```

应正常返回。

---

## 4、验证外部访问

浏览器访问：

```text
http://服务器IP:36002
```

应正常访问系统。

---

# 八、问题排查

## 1、502 Bad Gateway

原因：

Nginx 无法连接 Python 服务。

检查：

```bash
docker exec -it nginx-swagger curl http://172.17.0.1:36001
```

若失败：

* Python 未启动
* Python 未监听 `0.0.0.0`
* `proxy_pass` 配置错误

---

## 2、Connection Refused

检查：

```bash
ss -lntp | grep 36001
```

若显示：

```text
127.0.0.1:36001
```

说明仅监听本机。

应修改：

```json
"listen_host": "0.0.0.0"
```

---

## 3、Docker 无法访问宿主机

确认：

```bash
docker exec nginx-swagger curl http://172.17.0.1:36001
```

若失败：

检查：

```bash
ip addr show docker0
```

确认宿主机 Docker 网桥地址是否正确。

---

## 4、Nginx 配置未生效

检查：

```bash
docker exec nginx-swagger nginx -T
```

确认：

```nginx
proxy_pass http://172.17.0.1:36001;
```

是否为当前生效配置。

---

# 九、最终部署链路

```text
                Browser
                    │
             http://IP:36002
                    │
                    ▼
          Docker Nginx (36002)
                    │
                    ▼
      proxy_pass → 172.17.0.1:36001
                    │
                    ▼
      Systemd 管理 Python 服务
                    │
                    ▼
             IFAAS Packing
```

整个链路中：

* Python 服务仅负责业务逻辑。
* Systemd 负责服务保活。
* Docker Nginx 负责统一入口与反向代理。
* 浏览器仅访问 Nginx 暴露端口，无需直接访问 Python 服务。
