# No runtime dependencies.
# The Web and automation runtime supports Python 3.6.9+ and uses only the standard library.
# Optional MCP entry points have separate requirements and Python version constraints.
